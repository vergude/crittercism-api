<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="NavigationDrawerItem">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_size_large</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">@dimen/double_margin</item>
        <item name="android:minHeight">64dp</item>
        <item name="android:background">@drawable/bg_item_drawer</item>
    </style>

    <style name="SelectorCalendar">
        <item name="android:background">@drawable/bg_btn_arrows</item>
    </style>

    <style name="headGridView">
        <item name="android:textColor">@color/color_head_grid_view</item>
    </style>
</resources>
